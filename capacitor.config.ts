import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'APP.AWQATTRAVELAPP.APP',
  appName: 'أوقات للسياحة والسفر',
  webDir: 'dist'
};

export default config;
